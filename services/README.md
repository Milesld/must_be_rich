# ⚠️ 已封存（2026-07-21，路线图第 8 阶段）

本目录下的 9 个微服务是**未接线的架构原型**，不在维护范围内：

- 盘前服务用 `np.random.normal` 伪造 A50 数据；
- 日内服务行情硬编码为 `[]`；
- 模型从未从磁盘加载；
- 无任何生产流量或调用方。

**当前系统的主路径是 `research/` 研究链**（数据仓 → 回测 → 每日信号 →
模拟盘对账 → 手动下单），详见 `docs/REVIEW_AND_ROADMAP_2026-07.md` 的
2.6 节架构取舍：微服务/Redpanda/C++ 风控是「机构架构」，对单人 + 小资金
场景是负资产。

保留本目录仅作为将来（若有）服务化改造的参考骨架。**不要在这里新增
功能**；如需自动化，优先给 `scripts/update_data.py`、`research/paper_trading.py`
挂 cron。
