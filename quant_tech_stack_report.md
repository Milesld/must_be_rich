# A股量化交易系统技术栈推荐报告（2026年6月）

---

## 目录
1. [时序数据库](#1-时序数据库)
2. [实时数据流/消息队列](#2-实时数据流消息队列)
3. [特征存储](#3-特征存储)
4. [回测框架](#4-回测框架)
5. [模型框架](#5-模型框架)
6. [生产部署](#6-生产部署)

---

## 1. 时序数据库

### 推荐：DolphinDB（主力）+ ClickHouse（辅助分析）

### 详细分析

#### DolphinDB -- 中国量化领域的事实标准

DolphinDB 由浙江智臾科技开发（创始人周小华博士，前摩根士丹利量化系统架构师），已服务超过130家付费客户，包括幻方、九坤等头部量化私募和30多家顶级券商。虽不是开源的，但在中国量化生态中已是核心基础设施地位。

**具体客户案例**：
- **世纪前沿**：基于 DolphinDB 打造高性能私募投研数据中心，管理十年以上L2行情，秒级横截面数据加载
- **广州海浦**：管理超万个因子，处理每日百GB级高频数据
- **易方达基金、招商资管、广发资管**：构建公司级行情中心
- **长城证券、长江证券、广发证券**：高频量化投研中台

**性能指标**：
| 指标 | 传统架构 | DolphinDB |
|------|---------|-----------|
| 全市场回测 | 3-8小时 | **8分钟** |
| 实时因子计算延迟 | >100ms | **<=5ms** |
| 万级因子批量计算 | 小时级 | **分钟级** |
| 百亿级组合VaR | 秒级 | **0.5ms** |
| 存储压缩比 | 1x | **1:10** |
| 策略回测（100万次信号） | 53秒（backtrader） | **0.75秒** |

**独特优势**：
- 内置2000+金融专用函数，23种市场规则支持
- TSDB、OLAP、OLTP多模存储引擎统一
- 流批一体架构：同一代码兼容历史回测和实时计算
- 内置流计算引擎，延迟<=5ms，吞吐>100万条/秒
- 2026年正在推进GPU异构计算和AI融合（DeepSeek微调代码生成准确率提升40%）

**缺点**：
- 商业授权（不开源），有许可成本
- 生态相对封闭，社区主要由官方驱动
- 学习曲线存在，需要掌握其专用语言（类Python/SQL混合）
- 海外社区较小

#### ClickHouse -- 开源OLAP王者

**优势**：
- 吞入速度：2-4M点/秒（批量模式），压缩比10-30x（LZ4/ZSTD）
- 复杂聚合查询比TimescaleDB快3-10倍
- Materialized views可实时预计算OHLCV
- 分布式查询天然支持水平扩展
- Apache 2.0开源，被全球各大金融机构广泛采用

**适合A股场景**：
- 全市场回测（历史聚合查询极快）
- 合规报告（大规模数据聚合）
- 作为DolphinDB的辅助分析层

**缺点**：
- 无法高效处理逐笔单行插入（高频tick写入性能会下降）
- 无ACID保证，不适合订单/仓位状态管理
- JOIN能力相对有限

#### TimescaleDB -- 关系型+时序的混合方案

**优势**：
- 完全ACID合规（PostgreSQL生态）
- 完整的SQL支持，与订单/账户/持仓表直接JOIN
- PostgreSQL生态：pg_dump、ORM、已有运维经验
- v2.25在压缩数据上MIN/MAX查询提升289倍

**在A股场景的定位**：
- 适合交易记录、订单状态、账户管理等需要ACID保证的场景
- 不适合海量tick数据存储（吞入上限~100-500K点/秒，压缩比3-10x）

#### InfluxDB 3 -- 流数据专用

**优势**：
- Rust重写（Apache Arrow/Parquet基础），解决了v2的高基数问题
- Line protocol天然适合流式tick数据接入
- 10-20x压缩比，Grafana+Telegraf集成最流畅
- 实时监控面板和告警场景最优

**缺点**：
- 无原生关系型JOIN
- 中国本地化支持弱

### A股生产环境推荐架构

```
Market Data Feed
      │
      ▼
   Kafka Queue
      │
      ├──▶ DolphinDB (主力) —— 高频行情存储、因子计算、流批一体回测、实时风控
      │
      ├──▶ ClickHouse (辅助) —— 全市场OLAP分析、合规报告、冷数据查询
      │
      └──▶ TimescaleDB / MySQL —— 订单管理、持仓、账户等业务数据
```

**选择决策矩阵**：
- 有预算且追求极致性能：**DolphinDB**（主力）+ ClickHouse（辅助）
- 纯开源方案：**ClickHouse**（主力）+ TimescaleDB（业务数据）
- 个人/小团队起步：**TimescaleDB**（一体化，简单）或 ClickHouse（单机即可）

---

## 2. 实时数据流/消息队列

### 推荐：Redpanda（主力）+ NATS（内部服务通信）

### 详细分析

#### Kafka -- 生态标准，但JVM是隐忧

Kafka是中国量化私募中最广泛使用的消息队列，几乎所有系统架构文档都将其列为标准组件。Robinhood使用Kafka处理220万事件/秒。

**优势**：10年+生态，Confluent等商业支持，连接器最多

**劣势**：
- JVM GC导致尾延迟不可预测
- 需要KRaft/ZooKeeper协调，运维复杂
- Broker重启需数分钟
- 启用durability后延迟增加13.6倍

#### Redpanda -- 低延迟Kafka替代

**核心优势**：
- C++原生实现，无JVM，无GC停顿
- 平均延迟比Kafka低**10倍**（可达亚毫秒级）
- Broker重启<1秒（vs Kafka的数分钟）
- Thread-per-core架构消除锁竞争
- 兼容Kafka协议（可直接替换）
- 3-6倍更好的成本效率（同吞吐量用更少节点）

**适合A股量化场景**：
- 毫秒级行情数据分发
- 策略信号传输
- 需要可预测尾延迟的场景

**注意**：Redpanda从Kafka兼容协议出发，复杂Kafka部署迁移可能遇到兼容性挑战。

#### NATS -- 微秒级低延迟

**核心优势**：
- 单消息转发延迟可达**微秒级**
- 操作极简（"It is the low effort required for administration that really nailed NATS"）
- JetStream提供at-least-once持久化和消息重放
- Subject-based路由天然适合行情数据分发（如`level2.trans.600519`）

**生产案例**：
- FinecoBank（意大利最大在线券商）替换TIBCO Rendezvous为NATS
- INDMoney处理50万+tick/分钟，端到端延迟<200ms

**定位**：
- 适合策略引擎内部组件间通信（低延迟、简单）
- 不适合作为长期数据总线（持久化不如Kafka/Redpanda）

#### Apache Pulsar

**核心优势**：
- 计算和存储分离架构，支持弹性扩缩
- 内置多租户隔离
- 统一队列+流语义

**劣势**：
- 需要同时运维Broker+BookKeeper两层
- 生态连接器不如Kafka丰富
- 中国量化圈使用较少

### A股生产环境推荐架构

```
交易所/UDP组播
      │
      ▼
   FPGA解码
      │
      ▼
  Redpanda (消息总线) —— 替代Kafka，兼容Kafka协议，延迟更低
      │
      ├──▶ DolphinDB 实时流计算引擎
      │         │
      │         ▼
      │     因子计算 / 风控指标 (<=5ms)
      │
      ├──▶ 策略引擎 (C++进程)
      │         │
      │         ├──▶ NATS (内部pub/sub) —— 信号分发、订单路由、组件间通信
      │         │
      │         └──▶ QMT / 券商API ——> 交易所
      │
      └──▶ ClickHouse (批量写入) —— 历史分析
```

**分层决策**：
- 外部数据总线：**Redpanda**（低延迟+Kafka兼容，降低团队学习成本）
- 内部微服务通信：**NATS**（微秒级延迟，操作极简）
- 纯开源+BAT云原生：**AutoMQ**（Kafka API兼容+S3存储，成本更低）

---

## 3. 特征存储

### 推荐：自建方案（DolphinDB + Redis/Dragonfly）+ 轻量版Feast（可选）

### 详细分析

#### 中国量化私募的实际情况

中国量化私募**普遍不使用Feast/Tecton等商业化特征存储**，而是采用自建方案。原因：

1. **特征计算和存储一体**：DolphinDB内置的因子计算引擎本身就是一个特征平台
   - 同一套代码同时支持离线（历史回测）和在线（实时交易）特征计算
   - 因子版本管理和回滚通过数据库管理而非独立特征存储平台

2. **团队规模和成本**：
   - Feast需要1-2名专职ML基础设施工程师（$300K-500K总拥有成本/2年）
   - Tecton年费$80K-$500K+
   - 中国量化私募团队更倾向于用数据库+DAG调度替代商业化特征存储

3. **特征在线化模式**：
   ```
   离线特征计算链                 在线特征服务链
   ┌─────────────────┐           ┌─────────────────┐
   │ DAG调度(如Airflow)│            │ 行情Tick → 流计算  │
   │ → DolphinDB计算  │            │ → 实时特征更新    │
   │ → 写入Redis/     │            │ → Redis/Dragonfly│
   │   Dragonfly      │            │   (亚毫秒读取)    │
   └─────────────────┘           └─────────────────┘
   ```

#### Feast的实用场景

如果团队规模较大（5+个模型在生产环境），Feast仍然有价值：
- 点及时正确性（point-in-time correctness）：避免训练-服务偏差
- 特征复用：避免不同策略团队重复计算相同特征
- 在线存储统一抽象（Redis/Dragonfly）

**Feast落地关键**：
- 必须配置Redis Cluster（非单master）
- 强制TTL策略（`allkeys-lru`）避免存储膨胀
- 版本一致性在Online和Offline feature views之间强制同步

#### Hopsworks的潜在价值

如果团队关注**合规审计**（监管要求特征溯源），Hopsworks是唯一提供内置lineage/audit的开源选择。但中国量化私募尚未形成广泛采用。

### A股生产环境推荐架构

```
小型团队（<3个策略）：
    DolphinDB 因子计算 + Redis/Dragonfly 缓存 → 直接查询

中型团队（3-10个策略）：
    DolphinDB + Airflow/Dagster 调度 + Redis Cluster → 轻量自建

大型团队（10+策略，多研究员）：
    DolphinDB/ClickHouse + Feast(特征注册+服务) + Redis/Dragonfly
```

---

## 4. 回测框架

### 推荐：Qlib（ML驱动）+ vectorbt（快速扫参）+ 自研事件驱动（高频）

### 详细分析

#### 框架现状对比

| 框架 | 类型 | A股适配 | 速度 | 活跃度 | 推荐场景 |
|------|------|---------|------|--------|---------|
| **Qlib** | AI/ML全流程 | ★★★★ | 快 | ★★★★ | ML选股、因子挖掘 |
| **vectorbt** | 向量化 | ★★★（需自行准备数据） | ★★★★★ | ★★★★ | 参数扫参、快速验证 |
| **backtrader** | 事件驱动 | ★★★★（社区方案丰富） | ★★ | ★★（维护停滞） | 入门学习、中低频策略 |
| **hftbacktest** | 逐笔仿真 | ★★（需自建数据格式） | ★★★★★ | ★★★ | 高频策略、做市模型 |
| **聚宽/米筐** | 云端平台 | ★★★★★ | ★★★ | ★★★ | 快速原型、因子研究 |

#### 各框架详评

**Qlib（微软）** -- ML/AI驱动的A股回测首选
- 完整的因子挖掘→模型训练→回测→报告流程
- 内置LightGBM/XGBoost/深度学习集成
- 滚动训练、IC分析、组合优化
- 支持中国A股数据，集成RD-Agent自动化研发
- **缺点**：偏ML策略，传统规则型策略不如backtrader直观

**vectorbt** -- 参数优化极速方案
- 向量化计算（Numba JIT加速），比事件驱动快10-100倍
- 擅长参数网格搜索和热力图可视化
- 快速批量测试技术指标组合
- **A股局限**：需自行处理T+1、涨跌停规则，复杂止盈止损逻辑较难实现

**backtrader** -- 入门首选，但维护堪忧
- 19K+ GitHub Stars，中文资料最丰富
- 事件驱动逻辑清晰，学习曲线最低
- 搭配Tushare/AKShare即可跑通A股回测
- **关键问题**：作者已停止活跃维护，无新功能迭代
- **生产不建议**作为主力回测引擎，仅适合学习和原型

**hftbacktest** -- 开源最专业高频回测
- 逐笔仿真（tick-by-tick）+ 订单簿重建
- 队列位置模拟（订单成交概率建模）
- 延迟建模（行情延迟+订单延迟）
- Rust核心加速
- **A股阻碍**：需自备L2数据并转换格式，非即开即用

**聚宽（JoinQuant）/ 米筐（RiceQuant）** -- A股原生平台
- 聚宽：国内最大社区，200+因子库，免费可用，2023年底一创聚宽停服后官网回测仍可用，实盘需接PTrade
- 米筐：多市场覆盖（A股+港股+美股），学术级因子库，高级功能付费
- **适合**：快速验证想法、策略孵化，不适合生产级实盘

### A股生产环境推荐架构

```
策略研究阶段：
    Qlib (ML选股/因子挖掘) + vectorbt (快速参数扫参)
         │
         ▼
    聚宽/米筐 (快速验证A股特有规则、因子IC分析)
         │
         ▼
回测验证阶段：
    自研事件驱动引擎（中低频）或 hftbacktest（高频）
    需要模拟：T+1、涨跌停、停牌、财报规则
         │
         ▼
实盘执行：
    QMT (迅投) / MiniQMT — 对接券商实盘
    或 vn.py — 开源CTA/期货
```

---

## 5. 模型框架

### 推荐：LightGBM（主力表格模型）+ Qwen3系列/DeepSeek（NLP信号）+ PatchTST（时间序列实验）

### 详细分析

#### 梯度提升树（GBDT）-- 仍然是表格数据的王者

2025-2026年多项学术基准和实际应用一致证实：**调优良好的GBDT仍然在表格数据上超越深度学习**。

| 算法 | 推荐场景 | 速度 | 精度 |
|------|---------|------|------|
| **LightGBM** | 主力，大数据集 | 最快 | 第一梯队 |
| **CatBoost** | 高基数类别特征 | 中等 | 略优（类别特征） |
| **XGBoost** | 正则化强，生态成熟 | 快 | 第一梯队 |

**对于A股量化**：
- 选股模型：LightGBM是第一选择（训练速度、AUC均优秀）
- 因子包含大量类别特征（行业、概念板块、ST状态）：CatBoost略优
- 需要可解释性：EBGM（Explainable GBM）
- 需要不确定性估计：XGBoostLSS / NGBoost

#### 深度学习的时间序列模型 -- 实用还是过度？

**当前共识**：深度学习时间序列模型（TFT、N-BEATS、PatchTST）在金融预测上与调优良好的LightGBM的**实际差距很小**。深度学习在金融场景的价值更多体现在**非结构化数据**（新闻、公告、社交情绪）。

**PatchTST**：基于分patch的自注意力，2023年顶会论文，是目前时间序列预测中SOTA架构之一。但在A股市场中，其优势主要体现在长序列预测，而量化选股的因子通常已经结构化得足够好。

**实操建议**：
- GBDT仍然作为特征→预测的主力模型
- 深度学习在以下场景尝试：另类数据（卫星图、供应链图）、NLP情绪信号
- 不要为了用DL而用DL -- 如果表格特征已经结构良好，GBDT足够

#### LLM/NLP for 中文金融情绪分析

2025-2026年中国金融NLP的格局：

| 模型 | 大小 | 特点 |
|------|------|------|
| **Qwen3-4B-financial-sentiment-grpo** | 4B | GRPO微调，CoT推理输出，金融领域专用 |
| **Qwen3-8B + rLoRA** | 8B | 84.15%准确率，超RoBERTa/BERT/Baichuan2 |
| **FinSenti-Qwen3-0.6B** | 0.6B | 极轻量，适合低延迟部署 |
| **DeepSeek-R1-Distill-Qwen-7B** | 7B | 蒸馏模型，38.87%提升 |
| **FinnewsHunter** | 多智能体 | 实时新闻情绪融合+Alpha因子挖掘 |

**生产推荐**：
- 实时低延迟场景：**FinSenti-Qwen3-0.6B**（INT8量化后可在CPU上运行，延迟<50ms）
- 批量日频信号：**Qwen3-8B rLoRA**（高准确率，GPU批量推理）
- 构建情绪Alpha因子：多模型集成（FinBERT + Qwen3 + DeepSeek）

### A股生产环境推荐架构

```
特征输入
    │
    ├── 结构化因子（行情/财务/技术指标）
    │       │
    │       ▼
    │   LightGBM (主力) + CatBoost (类别特征)
    │       │
    │       ▼
    │   选股信号 / 仓位权重
    │
    ├── 非结构化文本（新闻/公告/研报/社交媒体）
    │       │
    │       ▼
    │   FinSenti-Qwen3 (单篇情绪) → 聚合 → 情绪因子
    │       │
    │       ▼
    │   事件驱动信号
    │
    └── 另类数据（可选）
            │
            ▼
        PatchTST / GBDT → 另类Alpha因子
```

---

## 6. 生产部署

### 推荐：Docker容器化 + Kubernetes编排 + vLLM模型服务 + Prometheus/Grafana监控

### 详细分析

#### 实时推理服务

量化交易有三层不同的延迟需求：

**Tier 1: Tick-to-Trade（微秒级）**
- 延迟预算：<100微秒
- 技术方案：FPGA / C++编译代码 / 预计算查找表
- 深度学习不直接参与这一层

**Tier 2: 决策支持信号（百毫秒级）**
- 延迟预算：<100ms
- 技术方案：量化小模型（如0.6B LLM INT8量化）+ TensorRT + GPU
- vLLM在A10 GPU上可达243ms TTFT（Qwen3-8B级别）

**Tier 3: 批量研究推理（分钟级）**
- 标准vLLM / TGI服务即可
- 主要考虑吞吐量而非延迟

#### 模型服务框架选择

| 框架 | 适用场景 | 性能 |
|------|---------|------|
| **vLLM** | 主力，HuggingFace模型直接加载 | A30上7B模型~2000ms延迟，多并发到100+ |
| **Triton+TensorRT-LLM** | 追求极致低延迟+NVIDIA统一硬件 | 比vLLM快10-15%，但需数小时编译 |
| **SGLang** | 批量高吞吐 | 比vLLM快~29%吞吐量 |
| **TensorRT-LLM standalone** | 纯C++部署，极低延迟 | 仅限NVIDIA最新GPU |

**推荐**：
- 主力：vLLM（灵活性+性能平衡最好，部署简单）
- 极低延迟：TensorRT-LLM + Triton（对Qwen3-8B级别模型进行INT8量化编译）

#### 监控告警

**标准栈**：
```
交易系统 Metrics → Prometheus → Grafana 仪表板
                      │
                      ▼
                 Alertmanager → 钉钉/微信/短信/PagerDuty
                      │
                      ▼
                 Thanos (长期存储 + 全局视图)
```

**关键监控指标**：
- **Counter**：订单总数、错误次数、重启次数
- **Gauge**：当前持仓、PnL、CPU/内存使用
- **Histogram**：函数执行时间、网络延迟、行情处理耗时（推荐使用Histogram而非Summary）
- **模型监控**：Evidently AI 集成数据漂移检测、特征分布监控

**仪表板设计**：
1. 实时交易面板：账户总览、持仓分布、实时行情、信号强度
2. AI模型性能面板：预测vs真实散点图、特征重要性、预测分布（检测模型衰减）
3. 策略回测面板：净值曲线、月度收益热力图、滚动夏普比率

#### Docker/Kubernetes在量化场景

**推荐采用的模式**：
- 开发和CI/CD：Docker多阶段构建，极简镜像
- 生产编排：Kubernetes + HPA（市场波动时自动扩容）
- GPU管理：NVIDIA GPU Operator

**量化特有的注意事项**：
- 交易所托管机房可能不支持Kubernetes -- 纯Docker Compose + Ansible也是务实方案
- 关键路径（Tick-to-Trade）通常直接C++二进制部署在裸机上，不经K8s
- K8s主要用于研究环境、批量回测、模型训练、非关键路径微服务

### A股生产环境推荐架构

```
┌─────────────────────────────────────────────────────┐
│                    监控层                             │
│  Prometheus + Grafana + Alertmanager + Evidently     │
│  (系统指标 + 模型漂移 + 交易异常告警)                   │
├─────────────────────────────────────────────────────┤
│                    风控层                             │
│  独立风控进程 (C++) / gRPC 同步调用                  │
│  (止损/合规/熔断，<10ms 检查)                        │
├─────────────────────────────────────────────────────┤
│                    执行层                             │
│  订单路由 (C++ 裸机)                                 │
│  → QMT/券商API → 交易所                              │
├─────────────────────────────────────────────────────┤
│                    策略层                             │
│  ┌──────────────┬──────────────┬──────────────────┐ │
│  │ 中低频策略    │ 高频策略      │ NLP情绪策略       │ │
│  │ (Python)     │ (C++)        │ (Python+vLLM)    │ │
│  │ K8s Pod      │ 裸机二进制    │ K8s GPU Pod      │ │
│  └──────────────┴──────────────┴──────────────────┘ │
├─────────────────────────────────────────────────────┤
│                    数据层                             │
│  ┌──────────────┬──────────────┬──────────────────┐ │
│  │ DolphinDB     │ ClickHouse   │ Redis/Dragonfly  │ │
│  │ (主力时序)    │ (OLAP分析)   │ (热数据缓存)      │ │
│  └──────────────┴──────────────┴──────────────────┘ │
│           ↑ Redpanda/NATS 消息总线                    │
│           ↑ 行情数据源 (UDP/TCP)                      │
└─────────────────────────────────────────────────────┘
```

---

## 总结：A股量化交易系统推荐技术栈（2026年6月）

| 组件 | 商业方案（推荐） | 开源方案（备选） | 备注 |
|------|----------------|----------------|------|
| **时序数据库** | DolphinDB | ClickHouse + TimescaleDB | DolphinDB是中国量化事实标准 |
| **消息总线** | Redpanda Enterprise | Redpanda (OSS) / AutoMQ | 兼容Kafka协议，延迟降低10倍 |
| **内部通信** | -- | NATS | 微秒级低延迟 |
| **特征存储** | DolphinDB内置 | Feast + Redis/Dragonfly | 大多数团队自建 |
| **回测框架（中低频）** | -- | Qlib + vectorbt | Qlib ML驱动，vectorbt扫参 |
| **回测框架（高频）** | -- | hftbacktest | 需自建A股数据格式 |
| **回测平台（快速验证）** | 聚宽 / 米筐 | -- | A股原生，无需自建数据 |
| **ML模型（表格）** | -- | LightGBM + CatBoost | GBDT仍是王者 |
| **NLP（中文情绪）** | -- | Qwen3系列 / DeepSeek | GRPO微调，INT8量化部署 |
| **模型服务** | NVIDIA Triton | vLLM | vLLM灵活性+性能平衡最优 |
| **监控告警** | Grafana Cloud | Prometheus + Grafana + Thanos | 集成钉钉/微信告警 |
| **容器编排** | -- | Docker + K8s / Docker Compose | 关键路径裸机部署 |
| **实盘执行** | QMT (迅投) | vn.py | 国内券商主流方案 |

### 核心选择原则

1. **数据存储**：DolphinDB是第一选择（如果预算允许）。它在中国量化生态的优势不可替代：2000+金融函数、流批一体、极致的因子计算性能。

2. **消息中间件**：Redpanda正在成为Kafka的有力替代，尤其是对低延迟有要求的场景。兼容Kafka协议降低了迁移风险。

3. **回测框架**：没有一个框架能覆盖所有需求。Qlib（ML）+ vectorbt（扫参）+ 聚宽（快速验证）的组合是务实选择。高频策略需要自研引擎或hftbacktest。

4. **模型**：不要过早引入深度学习。LightGBM在结构化因子上的效果已经足够好。深度学习的主战场景是NLP情绪信号和另类数据。

5. **部署**：分层对待延迟需求。Tick-to-Trade走裸机C++，决策信号走容器化，批量研究走K8s。不要把所有东西都塞进K8s。

---

**参考来源**：
- DolphinDB官方客户案例（世纪前沿、广州海浦等）及2025-2026技术路线图
- ClickHouse、TimescaleDB、InfluxDB官方文档及2025基准测试
- 《2024-2025年度中国量化投资白皮书》
- Redpanda、NATS、Apache Pulsar官方对比文档
- Feast、Tecton、Hopsworks特征存储平台对比分析（阿里云开发者社区2025）
- backtrader/vectorbt/Qlib GitHub仓库及社区活跃度
- ACM/Nature 2025-2026 GBDT与深度学习表格模型对比研究
- Qwen3、DeepSeek金融情感分析学术论文（2025-2026）
- vLLM vs Triton 推理服务器基准测试（2026）
- NVIDIA金融AI模型蒸馏最佳实践（2025）
- Conf42 Kube-Native 2025量化交易LLM系统演讲
