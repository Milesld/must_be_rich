# 待办：全库审查问题清单（2026-07-23 审查）

> 来源：2026-07-23 全库审查（策略方法论 + 代码 bug + 改进建议）。
> 所有高危项已逐一读码验证。逐项分析后请勾选 `- [x]`，或标注结论（确认/误报/已修复）。

---

## A. 策略 / 方法论问题

- [ ] **A1. 配置与研究纪律脱节：两池因子完全相同且未通过 IC 门禁**
  - 证据：`configs/strategy_bank_westock.yaml` 与 `configs/strategy_semiconductor_westock.yaml` 第 100-146 行启用因子及权重逐字一致；bank 配置注释仍写"半导体中期趋势核心"。
  - 矛盾点：momentum_60d(0.4) / ma_alignment(0.209) 为主，但 2026-07 首跑 IC 结论：动量类因子两池 ICIR 均为负；银行池仅 volatility/amplitude/atr/bollinger/rsi 通过。
  - 处置方向：按 IC 报告回写两池 factors 配置，或明确记录"暂时人工选择"的理由。

- [ ] **A2. 动态宇宙仍用当前成分，幸存者偏差未解**
  - 证据：`research/universe.py:8-12`、`research/westock_source.py:129` 返回当前成分；README 第六节自认 `universe.py` 未接入 `constituents_asof`。
  - 影响：所有回测收益系统性偏高，偏高幅度未知。
  - 处置方向：成分快照（`data/warehouse/constituents/`）攒够后接入 `filter_universe_pit`；接入前回测结论标注偏差上限。

- [ ] **A3. 统计基础偏薄**
  - top_n=3、银行全池约 42 只、3 年训练仅 36 个调仓点、验证区间仅 6 次调仓；WF 仅约 8 个测试窗。
  - 处置方向：对优化器结果降权采信；考虑扩大宇宙（多板块）或降低对单池权重搜索的依赖。

- [ ] **A4. multi_sector 配置硬矛盾**
  - 证据：`configs/strategy_multi_sector_westock.yaml` 仅 2 个板块（29-31 行）× `max_per_sector=6`（43 行）= 最多 12 只 < top_n=20 → 仓位上限仅 60%。
  - 处置方向：增加板块数或调小 top_n / 调大 max_per_sector。

- [ ] **A5. regime breadth 维度宽基路径下恒 0.5**
  - 证据：`core/portfolio/regime.py:253-287` 宽基 market_df 只有 close 列，breadth 恒 0.5，但配置仍给 breadth 0.15 权重 → 综合评分被稀释。
  - 处置方向：宽基路径下去掉 breadth 权重，或接入真实市场宽度数据。

---

## B. Bug 清单

### 高危

- [ ] **B1. 缺数据股票在反向因子上排第一**
  - 位置：`research/run_backtest_demo.py:1169-1171`（缺失/异常填 0.0）+ `:352-354`（反向因子 1-rank）。
  - 机制：0.0 是截面最小值 → 反向（pb/pe_ttm/peg/volatility 等）反转后得分最高 → 缺财报股票被当"最便宜"优先买入。回测/选股/模拟盘三处共用，同错。
  - 修法：缺失填 NaN，`_score_stocks` 内 dropna（该行已是 dropna 逻辑，只需上游别填 0.0）；或对 0.0 值做有效性校验。

- [ ] **B2. 复权重拉被段缓存绕过 → 混合复权基线永久入仓**
  - 位置：`scripts/update_data.py:93-98`（rebase 后全量重拉）+ `research/westock_source.py:262`（段缓存 TTL 3650 天）。
  - 机制：全量重拉命中旧缓存段（旧基线），只有含 today 的段是新基线；段缝检查阈值约 17% 测不出小额分红；尾部一致后永不复检。
  - 修法：rebase 检出后先清除该股 kline 缓存（如新增 `_purge_kline_cache(code)`）再重拉。
  - 附带：需检查现有仓库是否已有混合基线股票（可全量校验相邻交易日收益率异常）。

- [ ] **B3. regime 海外维度前视**
  - 位置：`core/portfolio/regime.py:307` `d <= as_of_date` 含当日；数据加载 `run_backtest_demo.py:844-872` 按海外市场本地日期键入。
  - 机制：美股 T 日行情在 A 股 T 日收盘后约 13 小时才产生；权重 0.30 且触发紧急减仓（`run_backtest_demo.py:167-188`）→ 回测仓位控制被未来信息美化。
  - 修法：海外数据键前移一日（T 日决策只用 ≤T-1 的海外收盘）。

- [ ] **B4. ST 涨跌幅 override 静默失效**
  - 位置：`configs/boards/sse_main.yaml` 条件 `trade_date < '2026-07-06'` + `core/backtest/constraints.py:301` eval + `:303` except 吞掉 TypeError。
  - 机制：Python 3 中 `date < str` 抛 TypeError → override 永不命中 → 主板 ST 始终按 ±10%（2026-07-06 前应 ±5%）。szse_main.yaml 需一并检查。
  - 修法：条件求值前把 trade_date 转 ISO 字符串，或配置条件改为可解析形式；并加一个"条件求值失败应告警而非 debug"的日志级别提升。

- [ ] **B5. 回测引擎卖出无持仓校验，超卖凭空创造现金**
  - 位置：`core/backtest/engine.py:513-525`（`cash += amount - total_cost` 不按实际持仓截断）；约束链 `constraints.py:236 check_all` 无持仓检查。
  - 修法：`_apply_fill` 卖出时 `filled = min(filled, positions.get(code, 0))`，超额部分拒单并告警。

### 中危

- [ ] **B6. 海外因子同日对齐前视**：`run_backtest_demo.py:1090, 1402-1408`（overnight_adr_mapped 等用 `overseas_data[trade_date]`）。与 B3 同源，因子层一并修。
- [ ] **B7. 动量 off-by-one**：`run_backtest_demo.py:1274`（及 1207/1227）`arr[-1]/arr[-min(w,len)]` 实为 w−1 日收益；应 `arr[-w-1]`。三处口径一致，属系统性偏差。
- [ ] **B8. 模拟盘影子账本卖单用成本价兜底**：`research/paper_trading.py:377-378` 持仓不在 top-N 时 prices 缺省 avg_cost → 影子成交定价错误、exec_gap_pct 基准错误、超配判断失真。
- [ ] **B9. settle 先成交后回填历史净值**：`paper_trading.py:432-433` + `update_nav:303-345`，历史净值被"未来"持仓回填。
- [ ] **B10. 真实账本买单不校验现金可为负**：`paper_trading.py:_apply_fill_to_book:172-177`，与"全或无"撮合规则矛盾。
- [ ] **B11. `_calc_min_lookback` 与 loader 窗口解析不一致**：`run_backtest_demo.py:1516-1532` 只扫 params 数值不解析因子名窗口（loader 1072-1080 会解析 momentum_60d→60）→ 数据跨度不足时长窗口因子静默恒 0.0 无告警。
- [ ] **B12. 印花税率无历史切换**：`core/backtest/cost_model.py:23` 固定 0.05%；2023-08-28 前应为 0.1%，跨该日的回测卖出印花税全部低估。
- [ ] **B13. 引擎费率覆盖参数无效**：`core/backtest/engine.py:214-252` 的 commission_rate/stamp_duty_rate 存了但从未传给 cost_model。
- [ ] **B14. ETF 识别不全误收印花税**：`cost_model.py:36,181-183` 仅 `startswith("51")`，深市 15xxxx / 科创 588xxx ETF 被误收 0.05% 印花税。

### 低危

- [ ] **B15. 末段当日 K 线被 3650 天缓存固化**：`westock_source.py:262`，收盘前运行会把不完整当日 bar 永久缓存。修法：含 today 的段 TTL 缩短或不缓存。
- [ ] **B16. 涨跌停基准价兜底错误**：`paper_trading.py:217-222` 信号日前停牌超 7 天时 prev_close 退化为 signal_close，复牌日涨跌停判定基准错。
- [ ] **B17. parquet/meta 写入非原子**：`research/warehouse.py:77, 232` 直接写，崩溃留截断文件；read_kline 无容错。修法：tmp+rename。
- [ ] **B18. rank_buffer 静默抬升**：`research/portfolio_rules.py:68` rank_buffer<top_n 时静默改为 top_n，配置错误无告警。
- [ ] **B19. report 打印条件作用域**：`paper_trading.py:490-491` `if buy_gaps else ""` 作用于整条 print，无买单时总体均值行丢失。
- [ ] **B20. 新股首日/次新版涨跌幅未处理**：`core/backtest/constraints.py:119` 注释称外部设 special=True，但全库无此路径；主板首日 ±44%、创业/科创前 5 日无涨跌幅会被误拒。
- [ ] **B21. 北交所 43xxxx 段未覆盖**：`constraints.py:29` / `rounding.py:40-43`，落入默认主板 ±10%。
- [ ] **B22. calendar 末日越界**：`core/common/calendar.py:104,111,121` is_month_end 等在缓存末日（2030 年末）抛 ValueError 而非返回 True。
- [ ] **B23. OHLC vwap 单位风险**：`engine.py:156` volume 若为"手"则 vwap 放大 100 倍被 clip 到边界价。确认上游单位。
- [ ] **B24. 滑点计入成本价又扣现金，realized_pnl 归因重复计一次**：`engine.py:505-508`（NAV 口径正确，盈亏归因失真）。

---

## C. 改进建议（优先级排序）

- [ ] **C1 (P0)** 修 B1：缺失因子值改 NaN + 打分 dropna。一行级修复，消除最严重选股错误。
- [ ] **C2 (P0)** 修 B2：rebase 检出后先清该股 kline 缓存再重拉；并对现有仓库做一次全量基线一致性校验。
- [ ] **C3 (P0)** 修 B3/B4：regime 海外键值前移一日；ST override 条件类型统一（含 szse_main.yaml 检查）+ 求值失败日志升 WARNING。
- [ ] **C4 (P1)** 修 A1：按 IC 结论回写两池因子配置；未回写前暂停用当前权重出实盘信号。
- [ ] **C5 (P1)** 修 B5/B12/B13：引擎卖出持仓校验；费率参数真正生效；印花税按日期分段。
- [ ] **C6 (P1)** 修 A2：宇宙接入 constituents_asof（先用已攒快照），消除幸存者偏差。
- [ ] **C7 (P2)** 模拟盘 sizing 对齐回测：`paper_trading.py:91-148` min_shares 硬编码 100 vs 回测 `strategy.min_shares`（run_backtest_demo.py:257/266）；regime 缩放与 max_weight_per_stock 分叉已文档化但 min_shares 未声明。
- [ ] **C8 (P2)** 修 B11：lookback 解析对齐 + 数据不足告警。
- [ ] **C9 (P2)** 修 B17/B18：写入原子化；rank_buffer 配置错误告警。
- [ ] **C10 (P3)** 测试基线确认：`python -m pytest tests/ -q`（基线 313 passed / 23 skipped）。本轮审查时套件在后台运行未捕获结果，修复后必须重跑全量。

---

## 附：审查时已验证无问题的点（不用再查）

- 因子打分链路三处（回测/pick_stocks/paper_trading）确实共用 `_score_stocks` + `select_target_portfolio`，无分叉（sizing 除外，见 C7）。
- 财报 PIT 严格 `announce_date < trade_date`（`run_backtest_demo.py:797-818`）。
- 因子打分只用昨日及以前数据（`get_prev_n_trading_days` 不含当日，`core/common/calendar.py:91-98`）。
- `filter_universe_pit` 严格 `< as_of_date`；factor_ic 前瞻收益对齐正确。
- 佣金最低 5 元、过户费、√252 年化、封板拒单逻辑无误。
