# 待办：全库审查问题清单（2026-07-23 审查 → 2026-07-30 复核并修复）

> 来源：2026-07-23 全库审查（策略方法论 + 代码 bug + 改进建议）。
> 2026-07-30：逐项读码复核 + 修复。24 条 bug 中 22 条成立、1 条误报（B24）、1 条描述需修正（B7 方向）。
> 测试基线：`python -m pytest tests/ -q` → **328 passed / 23 skipped**（修前 313 passed，新增 15 条针对性测试）。

---

## A. 策略 / 方法论问题

- [x] **A1. 配置与研究纪律脱节：两池因子完全相同且未通过 IC 门禁** — 确认
  - 复核：bank 与 semi 的 factors 段 6 个因子 enabled/weight/params 逐字一致；bank 注释确实写"半导体中期趋势核心"；README:143 记录了"动量类两池 ICIR 均为负"。
  - 处置：已修正错误注释，并在 `configs/strategy_bank_westock.yaml` factors 段顶部加入研究纪律警示（当前权重=优化器结果，与 IC 冲突，回写前不得据此出实盘信号）。
  - **未做**：没有按 IC 结论重写权重——那需要重跑 factor_ic/优化器后回写，凭空改数字不如显式记录冲突。**这是唯一留给人工决策的一项。**

- [x] **A2. 动态宇宙仍用当前成分，幸存者偏差未解** — 确认，已接入
  - 处置：`run_backtest_demo._build_monthly_universe` 每个月锚点先查 `warehouse.constituents_asof(board, anchor)`，命中就用当时成分，未命中回退当前成分，并打印命中率日志（早期月份必然未命中，偏差仍在但可量化）。

- [x] **A3. 统计基础偏薄** — 确认（原文数字偏乐观）
  - 实际：top_n=3、`pool_size: 30`（非 42）、wf_train_years=2 / wf_test_months=3 → 3 年数据约 **4 个**测试窗（非 8 个）。结论不变：优化器结果应降权采信。方法论项，不改代码。

- [x] **A4. multi_sector 配置硬矛盾** — 确认，已修
  - `max_per_sector: 6 → 10`（2 板块 × 10 = 20 = top_n），并注明板块数增加后可再调小。

- [x] **A5. regime breadth 维度宽基路径下恒 0.5** — 确认，已修（代码层）
  - 处置：`_calc_breadth_signal`/`_calc_valuation_signal`/`_calc_overseas_signal` 无数据时返回 `None`；`detect()` 剔除 None 维度并按剩余权重归一化，`dimension_scores` 仍以 0.5 展示便于报表。比改配置更通用（换数据源自动生效）。

---

## B. Bug 清单

### 高危

- [x] **B1. 缺数据股票在反向因子上排第一** — 确认（最严重），已修
  - 机制复核成立：`:1169/1171` 填 0.0 → features 无 NaN → `_score_stocks:347` 的 `dropna()` 形同虚设 → 0.0 落截面最低 → 反向因子 `1-rank` 翻成最高分。
  - 修法（与原文不同）：真正要改的是上游填值，改为 `np.nan`；`_score_stocks` 的 dropna 本来就在。

- [x] **B2. 复权重拉被段缓存绕过 → 混合复权基线永久入仓** — 确认，已修
  - `scripts/update_data.py` 检出 `rebase_detected` 后先 `_purge_kline_cache(code)` 再全量重拉。
  - **遗留**：现有仓库是否已存在混合基线股票尚未校验，需跑一次全量相邻交易日收益率异常检查。

- [x] **B3. regime 海外维度前视** — 确认，已修
  - `regime.py` 改为 `d < as_of_date`（原 `<=`）。配置 `overseas: 0.30` 确认（regime 内置默认 0.35 被配置覆盖）。

- [x] **B4. ST 涨跌幅 override 静默失效** — 确认，已修
  - `_resolve_override` 求值前把 trade_date 转 ISO 字符串（原来 `date < str` 抛 TypeError）；异常日志 debug → **warning**。sse_main/szse_main 两个配置都受影响，现已生效（新增 2 条测试）。

- [x] **B5. 回测引擎卖出无持仓校验，超卖凭空创造现金** — 确认（风险成立，当前调用方未触发），已修
  - `_apply_fill` 卖出按持仓截断、股数变化后重算成本、无持仓直接 rejected（新增 2 条测试）。

### 中危

- [x] **B6. 海外因子同日对齐前视** — 确认，已修：loader 改取 `< trade_date` 的最近一日海外数据（跨周末不丢信号）。
- [x] **B7. 动量 off-by-one** — 确认（原文结论对，修法方向写反了）：`len>=w+1` 时 `arr[-min(w,len)]`=`arr[-w]`，只跨 w−1 日。已改为 `arr[-w-1]`，三处（1274/1207/1227）同步。
- [x] **B8. 模拟盘影子账本卖单用成本价兜底** — 确认，已修：`cmd_signal` 对不在 top-N 的持仓补拉最近 30 天收盘价，仍缺失才退回 avg_cost 且打 warning。
- [x] **B9. settle 先成交后回填历史净值** — 确认，已修：`update_nav` 加 `until` 参数；`cmd_settle` 先用结算前持仓补齐 ≤ 最早挂单信号日的净值，再结算、再推进剩余日期。
- [x] **B10. 真实账本买单不校验现金可为负** — 确认，已修：撮合前按可用现金下调整手数（一手都买不起才拒单），`_apply_fill_to_book` 现金转负时告警。
- [x] **B11. `_calc_min_lookback` 与 loader 窗口解析不一致** — 确认，已修：抽出 `_factor_window(name, params)` 两处共用；`lookback_days` 不足最长因子窗口时 warning。
- [x] **B12. 印花税率无历史切换** — 确认，已修：2023-08-28 前 0.1%、之后 0.05%，按 `trade_date` 分段（engine/paper 都已传交易日；新增 3 条测试）。
- [x] **B13. 引擎费率覆盖参数无效** — 确认，已修：`TransactionCostModel(commission_rate=..., stamp_duty_rate=...)`，engine 默认改 `None`（None=按日期自动分段）。
- [x] **B14. ETF 识别不全误收印花税** — 确认，已修：基金代码段扩到 `51/56/58/15/16/18`（含科创 588、深市 15x/16x；新增 5 条测试）。

### 低危

- [x] **B15. 末段当日 K 线被 3650 天缓存固化** — 确认，已修：段结束日 ≥ today 时既不读也不写磁盘缓存。
- [x] **B16. 涨跌停基准价兜底错误** — 与 B8 同源，已随 B8 修复（signal_close 现在是真实收盘价，复牌日基准正确）。
- [x] **B17. parquet/meta 写入非原子** — 确认，已修：新增 `_atomic_write`（tmp+rename），meta/kline/成分快照三处都走；`read_kline` 对损坏 parquet 容错返回 None。
- [x] **B18. rank_buffer 静默抬升** — 确认，已修：`rank_buffer < top_n` 时打 warning。
- [x] **B19. report 打印条件作用域** — 确认（f-string 隐式拼接优先级高于三元），已修：拆成分支拼字符串。
- [x] **B20. 新股首日/次新涨跌幅未处理** — 确认（全库无 special=True 路径），已修：`update_daily_info` 支持行级 `no_price_limit=True` 跳过涨跌停登记，删除误导性注释（新增 1 条测试）。宇宙层次新过滤通常已排除这些标的。
- [x] **B21. 北交所 43xxxx 段未覆盖** — 确认，已修：`constraints.py` 与 `rounding.py` 都加 `43` 前缀（新增 1 条测试）。
- [x] **B22. calendar 末日越界** — 确认，已修：新增 `_next_trading_day_or_none`，`is_month_end/is_quarter_end/is_year_end` 在缓存末日返回 True 而非抛 ValueError。
- [x] **B23. OHLC vwap 单位风险** — 部分确认（westock 未声明 volume 单位），已修：vwap 落在 [low, high] 之外时判定不可用并退回 close（原来 clip 到边界价，会系统性按最高/最低价成交）。
- [x] **B24. 滑点/费用在 realized_pnl 重复计** — **误报**：买入费用计入 avg_cost、卖出时 `realized_pnl=(amount-total_cost)-avg_cost×shares`，展开后买卖费用各只扣一次；滑点只体现在 fill_price。无需修改。

---

## C. 遗留事项

- [ ] **C1. 按 IC 结论回写两池因子权重**（A1 未做部分）：需重跑 `factor_ic` + 优化器再回写；回写前不要用当前权重出实盘信号。
- [ ] **C2. 现有仓库基线一致性全量校验**（B2 遗留）：扫描所有 parquet 的相邻交易日收益率异常，找出历史遗留的混合复权序列并重拉。
- [ ] **C3. 模拟盘 sizing 对齐回测**：`paper_trading` min_shares 硬编码 100 vs 回测 `strategy.min_shares`；regime 缩放与 max_weight_per_stock 分叉已文档化但 min_shares 未声明。
- [ ] **C4. 修复后重跑回测对比**：B1/B3/B6/B7/B12 都会改变历史回测数值（尤其 B1、B7 直接改选股），需重跑并存档新基线，旧结论作废。

---

## 附：审查时已验证无问题的点

- 因子打分链路三处（回测/pick_stocks/paper_trading）共用 `_score_stocks` + `select_target_portfolio`，无分叉（sizing 除外，见 C3）。
- 财报 PIT 严格 `announce_date < trade_date`；因子打分只用昨日及以前数据。
- `filter_universe_pit` 严格 `< as_of_date`；factor_ic 前瞻收益对齐正确。
- 佣金最低 5 元、过户费、√252 年化、封板拒单逻辑无误。
- B24（realized_pnl 归因）经展开验算无重复计。
